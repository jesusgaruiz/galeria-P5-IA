function setup() {
    createCanvas(300, 240);
    noLoop();
  }
  
  function draw() {
    background(255, 200, 0);
    fill(0);
    textAlign(CENTER, CENTER);
    textSize(24);
    text("Sketch 1", width / 2, height / 2);
  }
  
